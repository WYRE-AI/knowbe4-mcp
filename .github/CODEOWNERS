# Workflow files require an explicit code-owner review — this is the one path
# where a malicious PR could exfiltrate secrets or tamper with the release
# pipeline, so it stays gated even though other paths do not require review.
/.github/workflows/ @asachs01
